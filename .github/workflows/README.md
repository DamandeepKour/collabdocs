# GitHub Actions

- `ci.yml` — lint, typecheck, unit tests, production build on push/PR
- `deploy.yml` — deployment checklist for Vercel + Neon

Vercel Git integration handles continuous delivery when the repo is imported.
